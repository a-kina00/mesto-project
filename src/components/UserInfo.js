export default class UserInfo {
  constructor({name, title}) {
    this._name = name
    this._title = title
  }

  getUserInfo() {
    const obj = {username, usertitle}
    return obj
  }

  setUserInfo() {

  }
}
